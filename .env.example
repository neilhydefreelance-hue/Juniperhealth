# Only needed on Cloudflare (build variables), not for local editing.
# Keystatic Cloud project, as "team/project".
PUBLIC_KEYSTATIC_CLOUD_PROJECT=
# Optional: force a storage mode ("local", "github" or "cloud").
# PUBLIC_KEYSTATIC_STORAGE=
